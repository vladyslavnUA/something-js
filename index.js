const addPls = (a, b) => {
    return (a + b);
}

const subtractPls = (a, b) => {
    return (a - b);
}

const multiplyPls = (a, b) => {
    return (a * b);
}

const dividePls = (a, b) => {
    return (a / b);
}

module.exports = {
    addPls,
    subtractPls,
    multiplyPls,
    dividePls
}